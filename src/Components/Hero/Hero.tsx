import React from 'react'

type Props = {
    title: string,
    subtitle: string,
    image: string,
    imageAlt: string,
}

const Hero = (props: Props) => {
    return (
        <div>Hero</div>
    )
}

export default Hero